console.log("javascript is connected...");
let add = document.getElementById('plus')
function add (addition) {
    let add = 0;
while(add< 100) {
    if (add % 2 === 0) {
        console.log("Even Num =", add)
    }
    // increment
    add++;
}